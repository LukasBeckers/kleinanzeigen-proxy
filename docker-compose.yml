services:
  kleinanzeigen-proxy:
    build: .
    ports:
      - "8001:8001"
    env_file:
      - .env
    volumes:
      - proxy-data:/data
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped

volumes:
  proxy-data:
